{
  "name": "shopping-reminder-function",
  "version": "1.0.0",
  "description": "Lambda function for sending shopping reminders.",
  "main": "dist/handler.js",
  "license": "Apache-2.0",
  "dependencies": {
    "aws-amplify": "^6.0.0",
    "@aws-amplify/api-graphql": "^6.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.92",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  }
}